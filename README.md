# web-audio-playground

Web Audio / Three.js scenes.

## Scenes

| Scene | Path |
|-------|------|
| Scene 1 — summer ’98 walk | [`scene1/`](./scene1/) |

## Local preview

```bash
npx http-server scene1 -p 8888 -c-1
```

Then open http://localhost:8888

> AudioWorklets need a real `http(s)` origin — don’t open `index.html` as a `file://` URL.
